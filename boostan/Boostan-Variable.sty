% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% حق نشر 1392-1402 دانش پژوهان ققنوس
% حقوق این اثر محفوظ است.
% 
% استفاده مجدد از متن و یا نتایج این اثر در هر شکل غیر قانونی است مگر اینکه متن حق
% نشر بالا در ابتدای تمامی مستندهای و یا برنامه‌های به دست آمده از این اثر
% بازنویسی شود. این کار باید برای تمامی مستندها، متنهای تبلیغاتی برنامه‌های
% کاربردی و سایر مواردی که از این اثر به دست می‌آید مندرج شده و در قسمت تقدیر از
% صاحب این اثر نام برده شود.
% 
% نام گروه دانش پژوهان ققنوس ممکن است در محصولات به در آمده شده از این اثر درج
% نشود که در این حالت با مطالبی که در بالا اورده شده در تضاد نیست. برای اطلاع
% بیشتر در مورد حق نشر آدرس زیر مراجعه کنید:
% 
% http://dpq.co.ir/licence
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                           Peoples      
% 
% - author
% - editor
% - manager
% \since{0.1.0}
\def\title#1{\gdef\@title{#1}}
\def\@title{%
	\@latex@error{No \noexpand\title given}\@ehc%
}

\def\subtitle#1{\gdef\@subtitle{#1}}
\def\@subtitle{
	\@latex@error{No \noexpand\subtitle given}\@ehc
}



\def\brief#1{\gdef\@brief{#1}}
\def\@brief{}

\def\location#1{\gdef\@location{#1}}
\gdef\@location{}

\def\date#1{\gdef\@date{#1}}
\gdef\@date{}


\gdef\@institute{پژوهکشده}
\def\institute#1{\gdef\@institute{#1}}

\gdef\@group{نام گروه}
\def\group#1{\gdef\@group{#1}}



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                           Peoples      
% 
% - author
% - editor
% - manager
% \since{0.1.0}
\makeatletter
\def\author#1{\gdef\@author{#1}}%
\def\@author{%
	\@latex@error{No \noexpand\title given}\@ehc%
}%
\makeatother

\makeatletter
\def\editor#1{\gdef\@editor{#1}}%
\def\@editor{%
	\@latex@error{No \noexpand\editor given}\@ehc%
}%
\makeatother

\makeatletter
\def\manager#1{\gdef\@manager{#1}}%
\def\@manager{%
	\@latex@error{No \noexpand\manager given}\@ehc%
}%
\makeatother



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%                           Project      
% 
% - Project
% - Type
% - Version
% \since{0.1.0}
\makeatletter
\def\type#1{\gdef\@type{#1}}
\def\@type{%
	\@latex@error{No \noexpand\type given}\@ehc%
}%
\makeatother

\makeatletter
\def\version#1{\gdef\@version{#1}}
\def\@version{%
	\@latex@error{No \noexpand\version given}\@ehc%
}%
\makeatother

\makeatletter
\def\project#1{\gdef\@project{#1}}
\def\@project{%
	\@latex@error{No \noexpand\project given}\@ehc%
}%
\makeatother

\endinput
